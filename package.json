{
  "name": "minihack-cohort2",
  "version": "1.0.0",
  "description": "Mini Hack Cohort 2 - Gaming and Gamified Solutions on Avalanche",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:fuji": "hardhat run scripts/deploy.js --network fuji",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.11.0",
    "express": "^4.18.0",
    "ws": "^8.16.0"
  },
  "engines": { "node": ">=20.0.0" }
}
